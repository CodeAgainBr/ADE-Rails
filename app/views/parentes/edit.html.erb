<section>	
	<div class="row my-3">
		<div class="col-11 mx-auto form-associado py-2">
			<h1 class="text-red-ade">Parente de <%= @associado.nome %></h1>
			<%= render 'form', parente: @parente %>
		</div>
	</div>
</section>

