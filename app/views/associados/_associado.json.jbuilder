json.extract! associado, :id, :nome, :data_nascimento, :local_nascimento, :rg, :cpf, :cep, :email, :cidade, :estado, :bairro, :rua, :numero, :telefone, :prato_preferido, :observacoes, :created_at, :updated_at
json.url associado_url(associado, format: :json)
